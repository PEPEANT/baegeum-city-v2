# Restored Asset Manifest Batches

Use this folder for temporary batch notes when many assets arrive at once.

Recommended batch note shape:

```text
batch-id:
received:
source:
files:
manifest candidates:
open questions:
```

Only `src/restored/assets/asset-manifest.js` is authoritative at runtime. Batch notes are planning aids, not live manifests.
